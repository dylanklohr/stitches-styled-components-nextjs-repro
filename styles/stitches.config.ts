import * as Stitches from '@stitches/react';

export const {
	css,
	styled,
	global,
	theme,
	keyframes,
	getCssString,
} = Stitches.createCss({
	theme: {
		colors: {
			red: 'rgb(255, 20, 20)',
		},
	},
});
